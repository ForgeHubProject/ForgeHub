DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-to-a-long-random-string"
PORT=3001

# Client / integration: base URL of this API (e.g. Git remote helper, CLI, Postman collection).
# FORGEHUB_API_URL="http://localhost:3001"

# Future: on-disk root for bare Git repos or large artifacts (not read by the API yet).
# GIT_STORAGE_ROOT="/var/lib/forgehub/git"
